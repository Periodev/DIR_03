[project]
name = "dir-solver"
version = "0.1.0"
description = "Offline shortest-solution and difficulty analysis tools for DIR levels."
requires-python = ">=3.12,<3.13"
dependencies = []

[project.scripts]
dir-solve = "solver.cli:main"
