# DIR 3.0：回收塊規則

- 版本：v0.1
- 狀態：第三區原型

## 地圖字元

- `A-Q`：普通塊。
- `R-Z`：回收塊。
- `a-q`：初始位於目標的普通塊。
- `r-z`：初始位於目標的回收塊。
- 同一字母的大小寫視為同一標籤，每張地圖不可重複。

第一、二區沒有 `R-Z/r-z` 時，行為完全不變。

## 資料結構

```gdscript
{
    "id": 1,
    "label": "R",
    "kind": "recovery", # normal / recovery
    "cell": Vector2i(1, 1),
    "vector": "Left",
}
```

## 行為

回收塊除「觸發方向撞到玩家」外，推動、安裝、觸發移動、碰撞與阻擋規則皆和普通塊相同。

普通塊觸發撞到玩家：

- 玩家與方塊皆不移動。
- 方塊方向保留。
- `install_order` 不變。

回收塊觸發撞到玩家：

- 玩家與方塊皆不移動。
- `player_queue` 無條件覆蓋為回收塊的方向。
- 回收塊方向清空。
- 回收塊移出 `install_order`。

牆、邊界、柵欄或無法推動的方塊仍會讓兩類方塊維持鎖定；只有回收塊直接撞到玩家才會回收。

## 玩家隊列擴充

第三區起，玩家隊列仍為容量 1，但覆蓋來源增加為：

1. 成功推動方塊取得的新方向。
2. 回收塊撞到玩家時交回的方向。
