# DIR 3.0：回收塊規則

- 版本：v0.2
- 狀態：第三區原型

## 地圖字元

- `A-Q`：普通塊。
- `R-Z`：回收塊。
- `a-q`：初始位於目標的普通塊。
- `r-z`：初始位於目標的回收塊。
- 同一字母的大小寫視為同一標籤，每張地圖不可重複。

第一、二區沒有 `R-Z/r-z` 時，行為完全不變。

## 資料結構

```gdscript
{
    "id": 1,
    "label": "R",
    "kind": "recovery", # normal / recovery
    "cell": Vector2i(1, 1),
    "vector": "Left",
}
```

## 行為

回收塊除「玩家空手時可用 X 取回向量」外，推動、安裝、觸發移動、碰撞與阻擋規則皆和普通塊相同。

### X 交互

| 玩家隊列 | 回收塊狀態 | X 結果 |
| --- | --- | --- |
| 有向量 | 空 | 將玩家向量安裝到回收塊。 |
| 空 | 有向量 | 將回收塊向量取回玩家。 |
| 空 | 空 | 無效。 |
| 有向量 | 有向量 | 無效，不覆蓋任一向量。 |

取回成功時：

- 玩家與方塊皆不移動。
- `player_queue` 原本必須為空，取回後改為回收塊的方向。
- 回收塊方向清空。
- 回收塊依 block id 從 `install_order` 移除，不要求位於隊首；其餘順序不變。

普通塊只能接受安裝，不能用 X 取回已安裝方向。

### 觸發碰撞玩家

普通塊與回收塊行為一致：

- 玩家與方塊皆不移動。
- 方塊方向保留。
- `install_order` 不變。

牆、邊界、柵欄、玩家或無法推動的方塊都會讓兩類方塊維持鎖定。觸發不再負責回收。

## 玩家隊列擴充

第三區起，玩家隊列仍為容量 1，來源為：

1. 成功推動方塊取得的新方向；此來源會覆蓋既有內容。
2. 玩家隊列為空時，用 X 從回收塊取回的方向；此來源不會覆蓋內容。
